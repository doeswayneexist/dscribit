// The function that adds the card to the database.
var addCard = function () {

	
}
// Check to see that the new card is a good card.

// Pass the new card to a php page that will store the card.

// Give the option to add another new card. 